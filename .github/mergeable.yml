version: 2
mergeable:
  - when: pull_request., pull_request_review.
    name: 'PR title check'
    validate:
      - do: or
        validate:
          - do: changeset
            must_include:
              regex: ['ipnext/arch/', 'ipnext/aas/']
              regex_flag: 'im'
              all: false
